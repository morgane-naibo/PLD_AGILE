package controller;

public class PointSelectionne extends Etat {
    public PointSelectionne(Controller controller) {
        super(controller);
    }

    // Pas de surcharge de handleActionB ou handleActionC car elles ne sont pas disponibles dans cet état
}
