package controller;

public class DemandeExportee extends Etat {
    public DemandeExportee(Controller controller) {
        super(controller);
    }
    // Pas de surcharge de handleActionB ou handleActionC car elles ne sont pas disponibles dans cet état
}
